object Androidx {
    private const val livedataVersion = "2.6.1"
    const val livedataCore = "androidx.lifecycle:lifecycle-livedata-core-ktx:${livedataVersion}"
    const val animation = "androidx.compose.animation:animation:1.4.3"
    const val livedataCompose = "androidx.lifecycle:lifecycle-viewmodel-compose:${livedataVersion}"
    const val livedataViewModel = "androidx.lifecycle:lifecycle-livedata-core-ktx:${livedataVersion}"
}